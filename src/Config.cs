using LightConfig;

namespace VDownload
{
    static class Config
    {
        // Main configuration file
        public static ConfigObject Main = new ConfigObject(Global.PATH_CONFIG, Global.CFGDATA);
    }
}
